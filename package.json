{
  "name": "@undefiorg/defarm",
  "version": "0.23.0",
  "main": "lib/index.js",
  "types": "lib",
  "license": "MIT",
  "author": "katopz",
  "scripts": {
    "gcp-login": "npx google-artifactregistry-auth",
    "test-dev": "jest --clearCache && jest --watch --runInBand --detectOpenHandles --no-cache ",
    "test": "jest",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "clean": "rm -rf lib",
    "coverage": "jest --coverage",
    "bump-minor": "git commit -am \"Version bump to $(npm version minor | cut -c 2-)\"",
    "bump-patch": "git commit -am \"Version bump to $(npm version patch | cut -c 2-)\"",
    "prebuild": "npm run clean && npm test",
    "build": "tsc --project tsconfig.build.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "dev": "tsc -b -w",
    "prerelease": "export VERSION=$(grep '\"version\"' package.json | cut -d '\"' -f 4 | head -n 1) && export GIT_MERGE_AUTOEDIT=no && echo Release $VERSION && git fetch . develop:main && git push --all --follow-tags && unset GIT_MERGE_AUTOEDIT",
    "release": "npm run build && npm publish --access=public"
  },
  "files": [
    "lib/**/*"
  ],
  "dependencies": {
    "@defillama/sdk": "^2.2.0",
    "@ethersproject/units": "^5.4.0",
    "@pancakeswap/sdk": "^2.3.2",
    "@uniswap/token-lists": "^1.0.0-beta.25",
    "@uniswap/v2-periphery": "^1.1.0-beta.0",
    "bignumber.js": "^9.0.1",
    "dotenv": "^10.0.0",
    "ethers": "^5.4.4",
    "lodash": "^4.17.21",
    "node-fetch": "^2.6.1",
    "web3": "^1.5.1",
    "web3-core-helpers": "^1.5.1",
    "web3-eth-contract": "^1.5.1"
  },
  "devDependencies": {
    "@types/jest": "^27.0.0",
    "@types/node-fetch": "^2.5.12",
    "@typescript-eslint/eslint-plugin": "^4.29.1",
    "@typescript-eslint/parser": "^4.29.1",
    "eslint": "^7.32.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.24.0",
    "firebase-functions-test": "^0.3.1",
    "jest": "^27.0.6",
    "nodemon": "^2.0.12",
    "ts-jest": "^27.0.4",
    "ts-node-dev": "^1.1.8",
    "tslint": "^6.1.3",
    "typescript": "^4.3.5"
  },
  "peerDependencies": {
    "ethers": "^5.4.4"
  }
}
