{
  "name": "@undefiorg/example-defarm-nodejs",
  "version": "0.1.0",
  "scripts": {
    "dev": "tsc -w",
    "test-dev": "jest --clearCache && jest --watchAll --runInBand --detectOpenHandles --no-cache ",
    "test": "jest",
    "up": "yarn upgrade ",
    "build": "tsc"
  },
  "dependencies": {
    "@undefiorg/defarm": "^0.10.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.4",
    "typescript": "^4.3.5"
  },
  "main": "bin/index.js",
  "types": "bin/index.d.ts",
  "files": [
    "bin"
  ]
}